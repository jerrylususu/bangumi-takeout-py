[project]
name = "bangumi-takeout-py"
version = "1.4.0"
description = ""
authors = [
    {name = "Neko Null", email = "jerrylu.mail@qq.com"},
]
dependencies = [
    "requests>=2.27.1",
    "tqdm>=4.63.0",
]
requires-python = ">=3.6"
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/jerrylususu/bangumi-takeout-py"

[dependency-groups]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.pdm]

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"
